# Release Notes

The release stream for Prompt Vault is documented here. Each entry summarises highlights, upgrade steps, and operational learnings
for a tagged version.

- [`notes.md`](notes.md) – Append a new section for every release prepared via `npm run release:prepare -- <version>`.

When publishing, ensure the changelog entry in the repository root references the relevant section in `notes.md` for deeper
context.
