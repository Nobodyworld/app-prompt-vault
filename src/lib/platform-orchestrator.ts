// App-local adapter for orchestrator SDK imports.

export { registerTool } from '@nw/orchestrator-sdk';
export type { ToolDefinition, ToolHandler, ToolResult, ToolContext } from '@nw/orchestrator-sdk';
