# Tools (app-prompt-vault)

Orchestrator-facing tool surface for Prompt Vault operations.

Authoritative implementation:

- Definitions + registration: `src/tools/index.ts` (`promptVaultToolDefinitions`, `registerPromptVaultTools`)

Docs:

- API/tool surface: `docs/API_REFERENCE.md`
